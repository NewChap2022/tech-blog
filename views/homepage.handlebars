    <ul class="post-list d-flex flex-column align-items-stretch">
        {{#each posts as |post|}}
        <li class="post">
            {{> post-list post}}
        </li>
        {{/each}}
    </ul>
