
    <h3 class="post-title">{{title}}</h3>
    <p class="text-end details">By <span>{{user.username}}</span> on <span>{{formatDate created_at}}</span></p>
    <p class="content">
    {{limitedText content}}
    <a href="/post/{{id}}">View More</a>
    </p>
